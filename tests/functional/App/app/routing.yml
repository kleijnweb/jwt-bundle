unsecured:
  path:      /unsecured
  defaults:  { _controller: TestBundle:Test:unsecured }

secured:
  path:      /secured
  defaults:  { _controller: TestBundle:Test:secure }

secured_with_secret_loader:
  path:      /secured-with-secret-loader
  defaults:  { _controller: TestBundle:Test:securedWithSecretLoader }