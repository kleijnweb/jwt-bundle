services:
  jwt.authenticator:
      class:  KleijnWeb\JwtBundle\Authenticator\Authenticator
      public: false

  jwt.user_provider:
      class:  KleijnWeb\JwtBundle\User\JwtUserProvider
      public: false

  jwt.security.authentication.provider:
    class: KleijnWeb\JwtBundle\Firewall\JwtAuthenticationProvider
    arguments:
        - '' # User Provider
    public: false

  jwt.security.authentication.listener:
    class: KleijnWeb\JwtBundle\Firewall\JwtAuthenticationListener
    arguments: ['@security.token_storage', '@security.authentication.manager']
    public: false